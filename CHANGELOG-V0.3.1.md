# v0.3.1

> 本版主题：**演示模式（幻灯片）体系重做** —— 版式推断改为注册表驱动、新增片段逐步显示与懒渲染；切换动画从"整页 mask 过渡"升级为**瓷砖转场**（每格持有前后两页的真实 DOM 克隆），动画菜单扩到 12 种。

## 新增功能

### 演示模式：版式注册表

- 版式推断由散落的 if 分支改为**有序规则表** `LAYOUT_RULES` + 唯一事实源 `LAYOUT_KINDS`：新增版式 = 注册一条规则 + 一段 CSS，不再改推断主流程
- `scanBlocks` v2：一次扫描产出块序列，版式推断与片段标记共用同一份结果
- 页级指令非法值告警并回落自动推断（不再静默失效）

### 演示模式：片段逐步显示（默认关闭）

- 生效版式限 content / content-list / roadmap；优先级链 = 页级注释 > HUD 运行时开关 > front matter
- 交互语义：`next` 先走片段再翻页（新页片段全隐）、`prev` 先退片段再翻页（目标页全显）、Home 全隐 / End 全显
- 开启途径三档：front matter `slideshow-fragments: on`（整副）、HUD 开关（本次播放）、页级 `<!-- fragments: on|off -->`
- **默认关闭**（用户拍板）：目录页也要用户频繁点击才显示，不够人性

### 演示模式：懒渲染

- 元数据全量、HTML 惰性（Map 缓存 + 窗口 `|i-h| ≤ 2`）；section key 含窗口态，进出窗口即重挂载，顺带清理 postRender 与片段残留
- 插件后处理与图片转 data URL 按 section 粒度执行（`data-processed` 防重复）

### 演示模式：瓷砖转场机制（本版核心）

- 整页（背景 + 内容）被切成 N 个单元，每格 = 一个 `overflow:hidden` 的窗口 + 一份【整页克隆】负偏移对齐：正面是旧页、背面是新页 ⇒ **每格都能独立做 2D/3D 变换、独立时序**
- 克隆取【活 DOM】（`cloneNode`）而非 HTML 字符串：活 DOM 已含 postRender 产物（mermaid SVG、katex）与已转好的图片 data URL，字符串两者都缺
- 克隆必须滤掉状态类（`ys-active / ys-past / ys-future / ys-leaving`），否则命中隐藏规则、窗口里是空的
- 新增 `src/lib/slideTiles.ts`（纯函数、零依赖、可 Node 直跑测试）：**切割形状**（方块 / 竖条 / 六边形）、**延迟节奏**（随机 / 波 / 行扫）、**单格时长**、**收尾融合**、**3D 冲量**全部配置化，CSS 只负责"怎么动"

### 演示模式：12 种切换动画

**柔和型 4**：水平滑动 / 淡入淡出 / 轻微缩放 / 无动画

**强烈型 8**：高斯溶解 / 百叶窗格 / 棋盘格子 / 百变立方 / 六角蜂巢 / 爆裂卡片 / 纵深穿梭 / 卡片推换

- **棋盘格子**：整页拆成方格，随机次序 3D 翻面露出下一页，随后融合回落
- **百变立方**：每格绕自身竖轴转 180°，三个面各沿法线平移半格宽 ⇒ 中途露出侧面（是立方体的棱在转，不是纸片在翻）
- **六角蜂巢**：尖顶正六边形严格无缝铺砖，各自随机次序翻面
- **爆裂卡片**：旧页碎片带随机方向旋转飞散，新页碎片从反方向飞入归位
- **纵深穿梭**：每格带不同纵深，翻面时前后穿梭产生视差
- **百叶窗格**（重做）：整页切成竖条，每条绕自身中轴翻面 —— 旧实现是整页 mask 竖叶，而页面背景与容器同色 ⇒ 背景完全隐形、只有内容在动
- **高斯溶解**（重做）：12 个软边水渍从多点渗开、互相吞并，有机边缘吞没整页，替换原"像素溶解"
- **卡片推换**（重做）：原"立方体"改为纯 2D 三阶段（缩 → 平移换位 → 放大），方向语义修正为大众翻页直觉
- 动画名称统一为**四字**（15 语言同步），"无动画"排在菜单最后

### 官网

- `index.html` / `help.html` 两页接入百度统计

## Bug 修复

- **演示模式目录页被误分段**：片段标记范围过宽，目录也逐条点击
- **翻页时窗口内 DOM 末页透出闪现**：用户报"以最后一页的谢谢观看作为过程动画，可是按顺序根本没到最后一页" ⇒ 非活动页默认全隐 + `ys-leaving` 机制（state 驱动，动画期间可见集合恰为 {旧页, 新页}）
- **mask 类动画"只有内容在动"**：页面背景与容器同色 ⇒ mask 孔洞露出同色背景、背景完全隐形；棋盘/百叶窗因此重做为瓷砖转场
- **六角蜂巢铺砖留空洞**：奇数行从 `x = w/2` 起铺 ⇒ 每行左缘留下楔形空隙、顶部三角也没补格，六边形退化成"带缝的方块"
- **六角蜂巢的影是矩形**：`box-shadow` 只画矩形边界 ⇒ 每个六边形外勾出方角，整片看起来是一格格方块（用户报"怎么是方块"）⇒ 改为每格垫一层六边形裁形的阴影层
- **卡片推换在暗色主题下变白刺眼**：白卡片写死 `#ffffff` ⇒ 改为跟随明暗模式；随后按用户反馈去掉白/黑舞台色，回归主题背景色
- **爆裂卡片 / 百变立方 / 纵深穿梭的起手帧与旧页对不上**（差异 11.24% / 4.09% / 跳变）⇒ 见踩坑 13

## 踩坑记录

### 13. `animation-delay` 未到时元素取【基值】——基值必须等于关键帧 0%

动画带延迟时，延迟相位内元素取的是**基值**（CSS 声明里的值），而不是关键帧的 0%。本版被它咬到四处：阴影层没设 `opacity: 0` ⇒ 延迟未到的格子第一帧就带着影；爆裂的背面基值 `opacity: 1` ⇒ 盖住正面露出新页；立方体卡片基值无 `transform`，而正面是静态 `translateZ(+半格宽)` ⇒ 起手整页被透视放大 1.068 倍；纵深穿梭 0% 是 `translateZ(-z)`、基值是 0 ⇒ 动画启动瞬间跳变。**规则：基值必须与关键帧 0% 一致。**

### 14. `mask-image` 是离散属性，切换点按【缓动后】的进度算

高斯溶解原本在 99% 关键帧切一次 mask 来"保证铺满"，但离散属性的切换点是关键帧区间的中点，而区间进度经过 easing 映射 —— ease-out 曲线下 99% 的偏移在**时间轴上约 150ms** 就触发了，水渍只存在一瞬就退化成单个大圆。**正解是让 mask 在终值处本身就铺满**（12 个水渍的核心半径 365px > 抖动网格半对角 316px），根本不需要切换。

### 15. `@keyframes` 只写 `to` 时，`from` 取底层值

想用一条 `to { filter: none }` 的收尾动画清掉残留的 `blur(0px)`，但底层本来就是 `none` ⇒ 该动画退化成 none→none 的空操作，压不住前一条 `forwards` 的填充值。**正解：该属性单独一条动画、且不设 `fill`**，播完自动回落基值。（另注：动画在层叠里压过内联样式，`el.style.filter = 'none'` 同样无效。）

### 16. 逐格 `filter: drop-shadow` = 每帧 N 次高斯模糊

六角蜂巢想让影沿六边形轮廓，先试了给每格挂 `drop-shadow`：88 个格子 = 每帧 88 次高斯模糊，**帧率从 60 掉到 34fps**。改用 `clip-path` + 半透明主题色的独立阴影层，零滤镜开销。**凡"每格一个 filter"的方案都要先量帧率。**

### 17. 正六边形铺砖：必须补出界格，且 `h` 与行距不能取整

相邻行靠半宽错位**互锁**，最上一行、最左一列、以及奇数行左缘的楔形空隙都要靠出界补格填上（`r`/`c` 从 -1 起）；`h = w·2/√3`、行距 `3h/4` 一旦取整，相邻行会互相压掉零点几像素（单测实测 9 个采样点落在两个六边形内）。补了【铺满性】单测（射线法逐点判"恰在一个六边形内"）——正是能抓住空洞的那条判据。

### 18. 变体类挂在容器自身 ⇒ 容器级规则要用 `.ys-deck.ys-anim-*`（无空格）

动画变体类挂在 deck 上，写 `.ys-anim-cube3d .ys-deck`（后代形式）永远不匹配，`filter` 静默不生效、无任何报错。

### 19. 3D 转场里改 `position` 会让容器高度塌陷

给 `.ys-deck.ys-anim-cube` 加 `position: relative`（本意是给舞台背景定位），特异性压过基类的 `position: absolute; inset: 0` ⇒ deck 退回正常流、高度塌成 0，子页 `inset: 0` 全部塌陷，**内容被顶到顶部溢出**。教训：容器级几何属性只能在基类定义。

### 20. 全屏页面切换场景下，3D 立方体的透视收益不足以压过"滑动"感知

3D 立方体路线迭代五轮（单面旋入 → 各绕自身边缘 → origin left 双面 → 全屏硬翻 → 三阶段缩-转-放）均被用户否决："还是和横向滑动很像"。根因：全屏尺度下透视收缩的相对量太小，人眼读作平移。最终以用户定义的纯 2D **卡片推换**替代；后来把同一套几何拆成**每格一个小立方体**（百变立方）反而成立 —— 单元尺度小，透视相对量才够。

## 技术改进

### 测试

- 新增 `tests/slides.test.ts`（分页 / 版式推断 / 指令解析，46 条）与 `tests/slideTiles.test.ts`（瓷砖几何与时序，14 条）；`npm test` 改为 `node --test` 三文件
- 瓷砖几何的判据强调"不变量"：方块/竖条的**无缝覆盖**、六边形的**恰一覆盖**、延迟模式的单调性

### 校验

- `check-i18n --strict` 接入 `npm run build`：15 语言零缺失 + 占位符一致才允许构建

### 文档与 skill

- `help.md` / `docs/slideshow-style-mapping.md` 更新动画双表格、片段默认关说明；`src-tauri/` 镜像同步
- `skills/slides-outline.md` + `skills/skills.json` v4：AI 提炼策略改为"默认零标记，用户明确要求分段演示才加 front matter"

## 其他

- 版本号 0.3.1（五载体同步：`package.json` / `src-tauri/tauri.conf.json` / `src-tauri/Cargo.toml` / `src-tauri/Cargo.lock` / `package-lock.json`）
- 多语言独立交叉审计：15 语言 × 515 键，缺键 / 多余键 / 空值 / 占位符不符**全部为 0**
- 取舍记录：删除"波浪"（与棋盘格子太像，用户判定重复）与"三角"两个变体
