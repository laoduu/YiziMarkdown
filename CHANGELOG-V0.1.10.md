# v0.1.10

## 新增功能

### 幻灯片版式系统升级（整页结构 → 14 种自动版式）

在 v0.1.9 的纯 Markdown 演示模式基础上，将版式推断从「只看页面首块」升级为「分析整页块结构」，补齐 PPT 常见版式，观感全面接近专业幻灯片。

- **整页结构推断**：不再只看第一块，而是分析整页的标题层级、块类型与组合方式，自动选择最合适的版式
- **14 种自动版式**：
  - `cover` 封面页（`#` 标题 + 副标题段落，居中大标题 + 渐变下划线）
  - `section` 章节过渡页（`##` 单独成页，大号序号 + 居中标题）
  - `thanks` 结尾页（`# 谢谢 / Thanks / Q&A`，居中收尾）
  - `agenda` 目录页（`## 目录` + 有序列表，大号数字双列网格）
  - `content` 内容页（标题左上 + 强调下划线，正文左对齐）
  - `content-list` 列表页（标题左上 + 大号列表）
  - `table` 数据表页（表头主题色 + 斑马纹行）
  - `roadmap` 路线图页（任务列表大号勾选框）
  - `figure` 图文页（左图右文 45/55 双栏）
  - `image` 图片页、`quote` 金句页、`code` 代码页、`chart` 图表页、`formula` 公式页
- **金句页重设计**：去掉左侧竖线；内容上下居中；大引号改为对角装饰——上引号定位在文案左上角外侧、下引号定位在右下角外侧（6em、35% 透明度），真实引用与强制版式同样生效
- **封面 meta**：front matter 提供 `author` / `date` 时，封面页底部自动显示「作者 · 日期」
- **显式指令**：`<!-- layout: xxx -->` 强制指定版式、`<!-- align: left|center|right -->` 整页对齐，覆盖自动推断（HTML 注释，渲染不可见）
- **装饰层**：左下角章节名 + 页码页脚，底部主题色进度条随翻页增长

### 鼠标滚轮翻页

- 演示模式支持滚轮翻页：向下滚下一页、向上滚上一页
- 内容可滚动时先滚动内容，滚到上/下边界再翻页；翻页后短暂锁定，防触控板惯性连翻

## 文档

- `docs/slideshow-style-mapping.md` 重写为整页结构 → 版式对照表
- `src-tauri/templates/Slide-Template.md` 模板更新，覆盖全部新版式示例（含封面 front matter）
- 新增根目录 `slideshow-test.md` 版式测试文档（14 种版式 + 显式指令示例）
