# v0.2.2

## 新增功能

### 工具栏交互重构

- **悬浮菜单全面改造**：导出菜单、表格选格器、代码块菜单、模板菜单由点击展开改为鼠标悬浮展开（100ms 延迟防抖），移除全屏遮罩层，交互更流畅
- **格式工具栏折叠**：窗口宽度 < 900px 时自动折叠格式工具栏；新增展开/收起按钮（ChevronsLeft/ChevronsRight 图标），支持手动切换

### 链接/图片对话框

- **通用对话框组件**：新增 `Dialog.tsx` 模态容器，统一背景遮罩、居中弹窗、ESC 关闭
- **链接插入对话框**：`LinkModal.tsx`，工具栏链接按钮改为打开对话框（输入 URL + 文本）
- **图片插入对话框**：`ImageModal.tsx`，支持网络 URL 和本地文件两种方式；本地文件通过 Tauri 原生文件选择器（`pick_image_file` 命令），非 Tauri 环境自动降级为浏览器 `<input type="file">`

### AI 功能增强

- **上下文轮数配置**：设置 → AI → 上下文轮数（0/1/2/3/5/10/20，默认 3），控制引用文档时发送的历史对话轮数
- **供应商名称 i18n**：`ProviderConfig` 新增 `i18nKey` 字段，设置面板和聊天面板均显示本地化供应商名称
- **供应商配置缓存**：切换供应商时自动保存当前配置（模型、Base URL、协议格式），切回时自动恢复

### 代码块增强

- **语法高亮**：集成 highlight.js，预览/并排/实时模式下代码块自动语法着色
- **代码块头部**：左上角显示语言标签，右上角新增复制按钮和自动换行切换按钮
- **复制到剪贴板**：一键复制代码块内容

### 演示模式改进

- **退出按钮**：鼠标活动时右上角显示半透明退出按钮（含 X 图标 + 文字），1.5 秒无操作自动隐藏
- **窗口状态还原**：进入演示前记录窗口状态（全屏/最大化），退出时自动还原到之前的窗口状态
- **Frontmatter 过滤**：预览/并排/实时模式下自动过滤 YAML frontmatter

### 主题与样式优化

- **Liquid Glass 主题增强**：玻璃效果透明度提升（背景 40%→60%，边框 12%→25%），新增内发光和阴影层次
- **标签栏分隔线**：非首个标签左侧添加 1px 分隔线，视觉区分相邻标签
- **AI 聊天面板可调宽度**：拖拽左边缘调整宽度（默认 380px，最小 280px，最大 min(600px, 40% 窗口宽度)）

### AI 技能列表 UX

- **悬浮介绍面板**：技能菜单中 hover 技能名称时，右侧弹出固定定位的详细介绍面板
- **颜色区分**：技能名称使用 `sidebar-text`（次要色），摘要和描述使用 `editor-text`（主色）

## Bug 修复

- **修复 AI 上下文注入顺序**：技能提示词和文档上下文 system 消息从「对话历史之前」移到「对话历史之后、当前用户消息之前」
- **修复已安装版本技能加载失败**：`tauri.conf.json` 资源映射由 glob `skills/*` 改为目录 `skills`
- **修复 AI 思考内容不显示流式效果**：思考区块在流式传输期间自动展开，内容后显示脉冲光标
- **修复 AI 思考内容自动滚动**：思考区块流式传输期间移除内部 `max-h` 限制，由聊天容器统一滚动
- **Frontmatter 前导空行容错**：`---` 前允许空白行，Frontmatter 解析和预览过滤不再因空行失效
- **发送后清空技能标签**：消息发送后自动移除输入区的技能 tag 并重置技能状态

## 其他

- 应用图标更新
- 文档 i18n 补全
